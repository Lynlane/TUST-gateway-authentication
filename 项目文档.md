# TUST 网关认证助手项目文档

## 1. 项目概述

TUST 网关认证助手是一款用于校园网认证管理的桌面应用程序，旨在简化校园网络的认证流程，提供无感登录、维持认证、多IP组管理、参数配置及数据包更新等功能，提升用户的网络使用体验。

## 2. 技术架构

### 2.1 开发环境
- 开发语言：C#
- 框架：WPF 、.NET 8.0
- 版本控制：Git
- 第三方库：LibGit2Sharp (用于Git仓库操作)

### 2.2 项目结构
```
- dataUpdate.cs           // 版本更新与数据同步相关功能
- MainWindow.xaml         // 主窗口UI布局
- MainWindow.xaml.cs      // 主窗口业务逻辑
- res/                    // 资源文件目录
  - data/                 // 应用数据目录
  - gateway2.ico          // 应用图标
```

## 3. 核心功能模块

### 3.1 认证管理
- **自动认证**：支持配置在线/离线认证间隔，自动维持网络连接状态
- **多IP组管理**：支持两组IP配置，可分别进行认证状态监控
- **认证参数配置**：支持配置账号、密码、IP地址、MAC地址等认证所需参数
- **运营商选择**：支持联通、电信、移动及校园网等多种运营商选择

### 3.2 网络配置
- **IP地址获取**：自动获取当前网络接口的IPv4和IPv6地址
- **网络接口选择**：智能选择最佳网络接口（排除虚拟网卡、隧道等）
- **IP状态检查**：验证当前IP是否为活跃状态

### 3.3 参数管理
- **URL参数解析**：支持从官方认证URL中提取参数，自动填充到配置界面
- **参数保存与加载**：自动保存用户配置，下次启动时恢复
- **hotfix参数应用**：支持通过hotfix.txt文件批量更新配置参数

### 3.4 版本更新
- **更新检查**：通过Git仓库检查是否有应用更新或数据更新
- **数据更新**：支持从远程仓库同步最新数据文件
- **版本比较**：对比本地与远程的APP版本和数据版本

## 4. 界面说明

### 4.1 主界面
- 采用TabControl布局，分为主要功能区和其他选项区
- 支持窗口最小化到系统托盘，通过托盘菜单快速操作

### 4.2 核心界面元素
- **认证配置区**：账号、密码、IP地址等核心参数配置
- **状态日志区**：显示认证过程及系统状态信息
- **IP组管理区**：支持添加/删除第二组IP配置
- **更新管理区**：检查更新、应用更新的操作入口
- **URL解析区**：用于解析官方认证URL中的参数

### 4.3 界面定制
- 支持显示/隐藏IPv6地址配置项
- 支持显示/隐藏终端类型、运营商配置项
- 支持区分/合并IP组的终端类型配置

## 5. 关键代码解析

### 5.1 使用Git实现的版本更新机制
```csharp
// 检查更新实现
public async Task<UpdateCheckResult> CheckForUpdatesAsync()
{
    return await Task.Run(() =>
    {
        var result = new UpdateCheckResult();
        // 读取本地版本信息
        VersionInfo localVersionInfo = new VersionInfo();
        if (File.Exists(localVersionPath))
        {
            string localVersionContent = File.ReadAllText(localVersionPath).Trim();
            localVersionInfo = ParseVersionFile(localVersionContent);
        }
        
        // 获取远程仓库中的版本信息
        VersionInfo remoteVersionInfo = GetRemoteVersionFile();
        
        // 比较版本差异
        if (remoteVersionInfo != null)
        {
            result.HasAppUpdate = !string.Equals(localVersionInfo.AppVersion, remoteVersionInfo.AppVersion, StringComparison.OrdinalIgnoreCase);
            result.HasDataUpdate = localVersionInfo.DataVersion < remoteVersionInfo.DataVersion;
            // 设置版本信息...
        }
        return result;
    });
}
```

### 5.2 认证URL构建
```csharp
private Dictionary<string, string> CreateRequestParameters(bool isLogout)
{
    if (isLogout)
    {
        return new Dictionary<string, string>
        {
            ["callback"] = "dr1004",
            ["login_method"] = txtLoginMethod.Text,
            ["user_account"] = txtLogoutAccount.Text,
            ["user_password"] = txtLogoutPassword.Text,
            ["ac_logout"] = txtAcLogout.Text,
            // 其他注销参数...
        };
    }
    // 登录参数构建...
}
```

### 5.3 本机网络IP获取
```csharp
private (string ipv4, string ipv6) GetPreferredNetworkIPs()
{
    var nic = GetBestNetworkInterface();
    if (nic == null) return (GetFallbackIPv4(), GetFallbackIPv6());

    try
    {
        string bestIPv4 = nic.GetIPProperties().UnicastAddresses
            .Where(addr => addr.Address.AddressFamily == AddressFamily.InterNetwork)
            .Select(addr => addr.Address.ToString())
            .FirstOrDefault() ?? string.Empty;

        string bestIPv6 = nic.GetIPProperties().UnicastAddresses
            .Where(addr => addr.Address.AddressFamily == AddressFamily.InterNetworkV6)
            .Where(addr => !addr.Address.IsIPv6LinkLocal)
            .Select(addr => addr.Address.ToString())
            .FirstOrDefault() ?? string.Empty;

        return (bestIPv4, bestIPv6);
    }
    catch (Exception ex)
    {
        UpdateStatus($"获取首选IP错误: {ex.Message}");
        return (GetFallbackIPv4(), GetFallbackIPv6());
    }
}
```

## 6. 使用说明

### 6.1 基本配置
2. 输入账号和密码信息
3. 选择运营商类型
4. 点击"开始认证"按钮启动认证流程

### 6.2 自动认证设置
1. 勾选"启动时自动认证"选项
2. 设置在线认证间隔和离线认证间隔（秒）
3. 程序将根据设置自动维持网络连接状态

### 6.3 参数导入
1. 在"URL解析"区域粘贴官方认证页面的请求URL
2. 点击"解析URL参数"按钮自动填充配置参数

### 6.4 版本更新
1. 点击"检查更新"按钮检查是否有可用更新
2. 如有数据更新，点击"数据包更新"应用数据更新
3. 如有应用更新，点击"软件包更新"前往下载最新版本

## 7. 注意事项

1. 程序需要适当的文件系统权限以保存配置和更新数据
2. 版本更新功能依赖Git环境和网络连接
3. 如遇网络异常，请检查网络接口和IP地址配置
