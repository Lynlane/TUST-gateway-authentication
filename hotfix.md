
# 热更新机制
使用**LibGit2Sharp**库进行Git操作，结合Gitee仓库的指定分支实现**更新检查**和**热更新**。
- 更新检查：拉取该分支的version.txt文件，将其中的软件版本与数据包版本与本地对比。
- 热更新：拉取该分支的对应参数文件，并应用到当前软件相关参数。

- 将国内远程Git仓库作为热更新服务器，无需另维护服务器的同时，避免无法连接至Github的风险。
- 使用软件内置的Git功能，避免了对本机安装Git的要求且不会弹出控制台界面。



### 实机测试结果：
- 软件更新检查和热更新功能正常工作。
- 检查更新、下载热更新数据包的反应时间约为3(±3)s，可接受。
- 开启本机Git的全局代理不会影响相关功能。

### 用到Git的两个功能（位于dataUpdate.cs）
1. 更新检查机制
   - 从Gitee仓库（ https://gitee.com/Lynlane/TUST-gateway-authentication.git ）拉取 version.txt 文件
   - 使用稀疏检出，只拉取必要的版本文件，优化性能
   - 比较本地与远程版本，区分 应用包更新 和 数据包更新
2. 数据更新机制
   - 完整克隆仓库到临时目录
   - 将更新的数据包文件复制到应用程序的 res\data 目录
   - 保留本地 version.txt 中的 APP_VERSION ，更新 DATA_VERSION


### 设计考量
1. 性能优化
   - 使用稀疏检出减少网络传输量，提高更新检查速度
2. 可靠性设计
   - 临时保存并在进程级清除代理环境变量（ http_proxy 、 https_proxy 、 GIT_HTTP_PROXY 、 GIT_HTTPS_PROXY ），执行git操作后恢复。
   - 临时目录自动清理，保持系统整洁
   - 异常处理机制，确保更新失败时不会影响应用正常运行
3. 用户体验
   - 区分应用更新和数据更新
   - 应用更新：较大更新，引导用户前往Gitee发布页面手动下载
   - 数据更新：通过数据包一键自动更新
4. 版本管理
   - 分离应用版本和数据版本，支持独立更新
   - 保留本地应用版本，避免意外覆盖
   - 使用 hotfix 分支进行更新，确保更新的稳定性
5. 安全性
   - 仅拉取无需配置身份，且无法修改远程仓库中的内容，确保易用和安全




### 核心实现文件
- dataUpdate.cs ：实现了Git更新的核心逻辑，包括更新检查、仓库克隆、文件复制等
- MainWindow.xaml.cs ：提供了UI交互，包括更新检查按钮、应用更新按钮和软件包更新按钮

### 技术栈
- 使用 LibGit2Sharp 库操作Git仓库
- 结合 Process 类执行Git命令
- 异步编程模型，提高应用响应性

高效、可靠，同时保持了良好的用户体验，能够适应不同的网络环境和使用场景。


